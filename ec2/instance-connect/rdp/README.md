# Create EC2 instance with SSM and generate RSA keypair with .pem file

# Update SG rules for the EC2 instance to allow RDP on port 3389 from anywhere

# Upload .pem file to EC2 management console connect to decrpyt for the password

# Open Remote Desktop Connect and enter the public ipv4 address of the EC2 instance

# Use credentials - Administrator:########## to login 
